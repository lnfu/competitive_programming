#include <bits/stdc++.h>

using namespace std;

int main() {
    int n, m;
    while (cin >> n >> m) {
        vector<int> stone;
        for (int i = 0; i < m; i++) {
            int temp;
            cin >> temp;
            stone.push_back(temp);
        }
    }
    return 0;
}