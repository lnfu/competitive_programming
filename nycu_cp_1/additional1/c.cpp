#include <bits/stdc++.h>

using namespace std;

int main() {
    int t;
    cin >> t;
    while (t--) {
        int n, m;
        cin >> n >> m;
        vector<int> house;
        for (int i = 0; i < m; i++) {
            int temp;
            cin >> temp;
            house.push_back(temp);
        }

        
    }
    return 0;
}