#include <bits/stdc++.h>

using namespace std;

int nxt() {
	int x;
	cin >> x;
	return x;
}

int main() {
	ios::sync_with_stdio(0);
	cin.tie(0);

	int C, n;
	while (cin >> C >> n) {
		for (int i = 0; i < n; ++i) {
			;
		}
	}

	return 0;
}