#include <bits/stdc++.h>

using namespace std;

int main() {
    int N;
    cin >> N;
    while (N--) {
        int A;
        cin >> A;
        if (A % 2 == 0)
            cout << A << ' ';
    }
    return 0;
}